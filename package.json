{
  "name": "spool",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "The debugger for AI agents",
  "type": "module",
  "workspaces": [
    "packages/*",
    "adapters/*"
  ],
  "scripts": {
    "spool": "node --import tsx/esm packages/cli/src/index.ts",
    "test": "node --import tsx/esm scripts/run-tests.ts"
  },
  "engines": {
    "node": ">=20.6"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
