[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meterbility-agent"
version = "0.3.1"
description = "Meterbility SDK for Python — trace LLM agent runs into the local Meterbility store"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Meterbility contributors" }]
keywords = ["llm", "agent", "tracing", "anthropic", "observability", "debugger"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Debuggers",
]
# Core has zero runtime deps — everything is stdlib (sqlite3, hashlib,
# json, pathlib, os, uuid). The Anthropic wrapper imports `anthropic`
# lazily, only when the user actually calls trace_anthropic().
dependencies = []

[project.optional-dependencies]
anthropic = ["anthropic>=0.40.0"]
dev = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/HoneycombHairDevelopers/Meterbility"
Repository = "https://github.com/HoneycombHairDevelopers/Meterbility"
Documentation = "https://github.com/HoneycombHairDevelopers/Meterbility#readme"
Issues = "https://github.com/HoneycombHairDevelopers/Meterbility/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
meterbility_agent = ["py.typed"]
