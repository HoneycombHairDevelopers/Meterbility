#!/usr/bin/env bash
# Meterbility launcher — runs the TypeScript CLI via tsx. Works without a build step.
set -e
DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec node --import "tsx/esm" "$DIR/packages/cli/src/index.ts" "$@"
